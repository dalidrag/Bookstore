{
  "name": "api",
  "version": "0.1.0",
  "description": "database api for Bookstore site",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "DD",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^5.7.1"
  }
}
